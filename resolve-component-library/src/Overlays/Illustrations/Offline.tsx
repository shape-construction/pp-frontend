import React from 'react';
import { StatusOfflineIcon } from '../../Icons/solid';
import IconBadge from '../../IconBadge';

const Offline: React.FC = () => (
  <IconBadge>
    <StatusOfflineIcon />
  </IconBadge>
);

export default Offline;
